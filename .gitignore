# dependencies
node_modules/

# next.js build output
.next/
out/

# production
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.log

# local env / secrets — never commit
.env
.env.local
*.local

# python
__pycache__/
*.pyc

# pipeline intermediates (raw Apify dumps, review JSON)
raw.json
reviews.json

# pinterest pin assets (regenerate with scripts/make_pins.py)
assets/pins/

# raw crawl markdown (own site + venue party pages)
data/crawls/

# price-extraction intermediate (regenerate from crawl markdown)
data/party_extract.json

# monthly listing-health report (regenerate with scripts/verify_listings.py)
data/verify_report.json
.vercel
.env*
